package Condicionales

/**
 * Función principal que ejecuta el programa.
 * Demuestra una operación de comparación booleana simple.
 */
fun main() {
    // Imprime el resultado de la expresión booleana "1 es menor que 1".
    // Esta expresión evalúa a 'false' ya que 1 no es estrictamente menor que 1.
    println(1 < 1)
}